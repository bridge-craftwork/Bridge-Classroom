Entry-point screenshots for the /dev gallery (Tab 2), generated by `npm run dev:shots`.

Committed PNGs (A1.png, B1.png, …). Missing surfaces fall back to a "capture pending" panel in the gallery UI, so the server-backed B2/B3/C1 can stay uncommitted until their seeded-session capture lands.
