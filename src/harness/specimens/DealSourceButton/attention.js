// The priming state — when there's no deal this is the only thing worth clicking,
// so it goes primary and pulses (reduced-motion respected).
export default { label: 'attention · priming', props: { attention: true } }
