// Default density, all four rows — the review overlay as the solo table shows it.
// ddtricks packs 20 seat×strain cells over [N,S,E,W] × [S,H,D,C,NT]; this is a real
// payload from the solver for a live deal.
export default {
  label: 'full (4 rows) · contract matched',
  props: {
    ddtricks: '64748647487969579695',
    finalContract: { contract: '1H', declarer: 'S' },
  },
}
