// The bare cue form: no heading, set back visually — a status, not something to act on.
export default {
  label: 'waiting · untitled cue',
  props: { tone: 'waiting' },
  slots: { default: 'Waiting for the first deal…' },
}
